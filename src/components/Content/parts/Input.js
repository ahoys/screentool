import React from 'react';

const Input = React.createClass({

    render: function () {
        return (
            <div className="Input">

            </div>
        );
    }
});

export default Input;